const input = require('fs')
    .readFileSync('./dev/stdin.txt')
    .toString()
    .trim()
    .split('\n')[0]
    .split(' ')
    .map((e) => Number(e));

const ans = (input) => {
    const [M, N] = input;
    console.log(M);
    console.log(N);
    const ans = [];

    for (let i = 0; i < array.length; i++) {
        const element = array[i];
    }
};

ans(input);
